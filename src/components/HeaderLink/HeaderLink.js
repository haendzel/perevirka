import React from "react";
import { StyledHeaderLink } from "./HeaderLink.styled";

const HeaderLink = ({ children }) => {
  return <StyledHeaderLink>{children}</StyledHeaderLink>;
};

export default HeaderLink;
